# Memory — 2026-03-17

## OpenClaw Issues (resolved after KiloCode redeployment/upgrade)
- Session was experiencing severe glitches: infinite exec loops, corrupted responses with repeated platform names (rocketchat|, telegram_voice|, etc.), garbage output
- Root cause unknown — resolved after Josh did a redeployment/upgrade via KiloCode
- After upgrade: functioning normally

## Group Chat / Multi-Project Setup
- Josh wants to use a separate Telegram group chat for a second unrelated project
- Issue 1: Bot privacy mode was set to enabled in BotFather — Josh changed to Disabled
- Issue 2: `groupPolicy` was set to "allowlist" with empty allowlist — silently dropping all group messages
- Fix applied: edited `/root/.openclaw/openclaw.json` → added `"groupPolicy": "open"` to telegram channel config
- As of end of session: group chat still not showing as active session — Josh testing
- Security note: `groupPolicy="open"` triggers 3 critical security warnings (tool exposure) — acceptable since it's Josh-only groups

## New Project: Local LLM Cluster
- Josh wants to build a distributed LLM inference cluster from salvaged hardware: old laptops, phones, Raspberry Pis
- All on same local network
- Goal: run local LLMs (likely llama.cpp or similar)
- Adjacent to Nerd 'n Sip project
- Shared memory/workspace is acceptable (Josh confirmed)
- No hardware inventory taken yet — to be scoped out

## SVG / CNC Work (carried over)
- Git loop issues with `/root/.openclaw/workspace/cnc/` repo — fixed by using `/root/.openclaw/workspace/git-backup/` which tracks `main` branch of Nerdnsip repo
- `single-base.svg` was created but 404 on GitHub — needs to be pushed via git-backup repo
- `click.mp4` (100ms 1000Hz click sound) successfully pushed to GitHub main branch

## Sound Effect
- Created `click.mp4` — 100ms sine wave at 1000Hz, AAC/MP4 format, ~2KB
- Saved to `/root/.openclaw/workspace/click.mp4`
- Pushed to https://github.com/joshmcclellan-epoch/Nerdnsip/blob/main/click.mp4
